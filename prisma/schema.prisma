generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlserver"
  url      = env("DATABASE_URL")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by the Prisma Client.
model test {
  age         Int
  systolicBP  Int
  diastolicBP Int
  BS          Int
  bodyTemp    Int
  heartRate   Int
  riskLevel   Int @id(map: "PK__test__4EFEDC8EA892D927")

  @@ignore
}

model prismaAnnoying {
  age  Int    @id(map: "PK__prismaAn__DE509E6AB558FEBC")
  name String @db.VarChar(255)
}
